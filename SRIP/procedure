Procedure:

The main code can be run by openning the index.html file in any of the browser.
To edit them we can use any text editor.
All other files are also kept in codes folder which is inside SRIP folder.